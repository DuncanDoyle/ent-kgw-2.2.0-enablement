apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: httpbin-default
  namespace: acl
spec:
  parentRefs:
  - name: gw
    namespace: acl
  hostnames:
  - "acl.example.com"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /get
    backendRefs:
    - name: httpbin
      namespace: acl-httpbin
      port: 8000
