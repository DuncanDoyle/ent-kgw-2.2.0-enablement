apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: httpbin
  namespace: waf
spec:
  parentRefs:
  - name: gw
    namespace: waf
  hostnames:
  - "waf.example.com"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: httpbin
      namespace: waf-httpbin
      port: 8000
