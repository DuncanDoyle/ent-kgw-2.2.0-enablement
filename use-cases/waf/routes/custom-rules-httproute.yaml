apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: custom-rules
  namespace: waf
spec:
  parentRefs:
  - name: gw
    namespace: waf
  hostnames:
  - "custom-rules.example.com"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: httpbin
      namespace: waf-httpbin
      port: 8000
